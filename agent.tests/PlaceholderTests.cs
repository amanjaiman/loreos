namespace Lore.Agent.Tests;

/// <summary>Placeholder wiring test so <c>dotnet test</c> has something to run.
/// Real tests arrive with the first behavior spec (003+).</summary>
public sealed class PlaceholderTests
{
    [Fact]
    public void TestHarnessIsWired()
    {
        Assert.True(true);
    }
}
