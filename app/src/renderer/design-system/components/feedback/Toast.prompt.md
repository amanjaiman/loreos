Notification surface with a variant icon, title, and message. Render inside your own positioning container.

```jsx
<Toast variant="success" title="Saved">Your memory was indexed.</Toast>
<Toast variant="warning" title="Heads up">Capture is paused.</Toast>
```

Variants: `info` (default), `success`, `warning`, `danger`.
