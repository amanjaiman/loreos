User avatar; shows the image when `src` is set, otherwise derives initials from `name`.

```jsx
<Avatar name="Ada Lovelace" status="online" />
<Avatar src="/u/ada.jpg" name="Ada Lovelace" size="lg" />
```

Sizes: `sm` · `md` · `lg`. Status: `online` · `busy` · `away`.
