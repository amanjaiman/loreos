Boolean checkbox. Forwards native `<input type="checkbox">` props (`checked`, `defaultChecked`, `onChange`, `disabled`).

```jsx
<Checkbox label="Remember this device" defaultChecked />
<Checkbox label="Disabled" disabled />
```
