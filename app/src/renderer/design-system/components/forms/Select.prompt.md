Dropdown built on the native `<select>` with a custom chevron and the shared field shell.

```jsx
<Select label="Model" placeholder="Choose…" options={["Ambient", "Focused", "Deep"]} />
<Select label="Region" options={[{value:"us",label:"US"},{value:"eu",label:"EU"}]} />
```
