Icon-only button for toolbars and dense controls — always pass `label` for accessibility.

```jsx
<IconButton icon="settings" label="Settings" />
<IconButton icon="plus" label="New" variant="solid" />
```

Variants: `ghost` (default), `solid`. Sizes: `sm` · `md` · `lg`.
