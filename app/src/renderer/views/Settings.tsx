import { PageStub } from './PageStub';

export function Settings(): JSX.Element {
  return (
    <PageStub
      eyebrow="// preferences"
      title="Settings"
      note="Model, capture, appearance, data and about arrive in T010."
    />
  );
}
