import { PageStub } from './PageStub';

export function Connect(): JSX.Element {
  return (
    <PageStub
      eyebrow="// wire up your tools"
      title="Connect"
      note="Setup for Claude Desktop, Claude Code and Cursor arrives in T008."
    />
  );
}
