import { PageStub } from './PageStub';

export function Add(): JSX.Element {
  return (
    <PageStub
      eyebrow="// seed a memory"
      title="Add"
      note="Manually add a memory and import a document — arrives in T009."
    />
  );
}
