import { PageStub } from './PageStub';

export function Home(): JSX.Element {
  return (
    <PageStub
      eyebrow="// ambient status"
      title="Home"
      note="Recent highlights and terse stats arrive in T005."
    />
  );
}
