import { PageStub } from './PageStub';

export function Library(): JSX.Element {
  return (
    <PageStub
      eyebrow="// memory.index"
      title="Library"
      note="Search, view, edit and delete memories arrive in T006."
    />
  );
}
