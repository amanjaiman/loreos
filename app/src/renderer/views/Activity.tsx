import { PageStub } from './PageStub';

export function Activity(): JSX.Element {
  return (
    <PageStub
      eyebrow="// what lore saw"
      title="Activity"
      note="The transparency timeline of captured, skipped and filtered windows arrives in T007."
    />
  );
}
