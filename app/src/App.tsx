/** Placeholder shell. Onboarding, library, and settings arrive with spec 010. */
export function App(): JSX.Element {
  return (
    <main className="placeholder">
      <h1>Lore</h1>
      <p>Your personal memory layer. The app shell arrives with spec 010.</p>
    </main>
  );
}
