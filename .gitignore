# .NET
bin/
obj/
*.user
.vs/

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Electron
node_modules/
.webpack/
out/
dist/
*.log
.eslintcache

# Packaging (spec 011): PyInstaller / setuptools / forge build artifacts
build/
# Staged native payload assembled by installer/build.ps1 (agent + CLI + memoryd + skills)
/app/native/

# OS noise
.DS_Store
Thumbs.db

# Local working documents — PLAN.html references the legacy project URL,
# which constitution §4.1 bans from the repo.
PLAN.html
